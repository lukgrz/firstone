package com.kodilla.good.patterns.challenges.productOrderService;

public class UserOrderService implements OrderService {
    public boolean order (User user, Basket basket) {
        return true;
    }
}
